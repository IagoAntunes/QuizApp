import 'package:flutter/material.dart';

class ImagePoples extends StatelessWidget {
  const ImagePoples({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      "assets/images/peoples.png",
    );
  }
}
